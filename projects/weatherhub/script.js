const cityInput = document.getElementById("city"); const searchButton = document.getElementById("search"); const weatherContainer = document.getElementById("weather"); searchButton.addEventListener("click", () => { const city = cityInput.value; getWeather(city); }); function getWeather(city) { fetch(`https://api.open-meteo.com/v1/forecast?latitude=52.52&longitude=13.41&current_weather=true&hourly=weathercode,temperature_2m,precipitation&daily=weathercode,temperature_2m_max,temperature_2m_min,precipitation_sum&timezone=auto`) .then(response => response.json()) .then(data => { const weatherData = data; renderWeather(weatherData); }); } function renderWeather(weatherData) { const weatherHTML = weatherData.daily.time.map((day, index) => { return `<div class="weather-card"> <h2 class="text-2xl font-bold">${day}</h2> <p class="text-lg">Temperature: ${weatherData.daily.temperature_2m_max[index]}°C</p> <p class="text-lg">Precipitation: ${weatherData.daily.precipitation_sum[index]} mm</p> </div>`; }).join(""); weatherContainer.innerHTML = weatherHTML; }